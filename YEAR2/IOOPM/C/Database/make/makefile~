CC=gcc
CFLAGS=-Wall -ggdb -std=c99

all:	databas

databas:	db dbcode
	$(CC) $(CFLAGS) -o databas db.o dbcode.o

db:	db.c
	$(CC) $(CFLAGS) -c db.c

dbcode:	dbcode.c
	$(CC) $(CFLAGS) -c dbcode.c

run:	all
	./databas databas.db

clean:
	-rm -rf *.o
