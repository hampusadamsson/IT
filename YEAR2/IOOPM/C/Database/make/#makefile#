CC=gcc
CFLAGS=-Wall -ggdb -std=c99

all:	databas

databas:	db.o dbcode.o
	$(CC) $(CFLAGS) -o databas $^

db.o:	db.c
	$(CC) $(CFLAGS) -c db.c

dbcode.o:	dbcode.c
	$(CC) $(CFLAGS) -c dbcode.c

run:	all
	./databas databas.db

clean:
	-rm -rf *.o

